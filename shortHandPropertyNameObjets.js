//----------sin shortHandProperty---------//
const name = 'laptop'
const price = 3000

const newProduct = {
    name: name, //vease que se repite name dos veces
    price: price// igual que el price
}
console.log(newProduct)

//--------con shortHandProperty------------//
const name2 = 'laptop'
const price2 = 3000

const newProductShort = {
    name2,   // como la propiedad y el valor tiene mismo nombre .. se coloca una sola vez (name2)
    price2
}
console.log(newProductShort)



